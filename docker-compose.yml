services:
  portfolio-fe:
    image: ngothanhdatak/portfolio:latest
    container_name: portfolio-app
    ports:
      - "5173:5173"
    restart: unless-stopped
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
